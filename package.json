{
  "name": "ts-lambda-template",
  "version": "1.0.0",
  "description": "Typescript lambda template with tsup for bundling.",
  "scripts": {
    "build": "tsup && pnpm bundle",
    "bundle": "cd dist && zip -r index.zip index.js*",
    "build:watch": "tsup src/index.ts --watch",
    "format": "prettier --write ."
  },
  "types": "dist/index.d.ts",
  "main": "dist/index.js",
  "tsup": {
    "entry": [
      "src/index.ts"
    ],
    "clean": true,
    "minify": true
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.109",
    "lefthook": "^1.2.6",
    "prettier": "^2.8.1",
    "tsup": "^6.5.0",
    "typescript": "^4.9.4"
  }
}